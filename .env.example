# NexusGate Server Configuration

# Server listen address
LISTEN_ADDR=:8080

# PostgreSQL database
DB_HOST=localhost
DB_PORT=5432
DB_USER=nexusgate
DB_PASSWORD=nexusgate
DB_NAME=nexusgate
DB_SSLMODE=disable    # Use "require" in production

# MQTT broker (Mosquitto)
MQTT_BROKER=tcp://localhost:1883

# JWT secret — CHANGE THIS in production
JWT_SECRET=change-me-to-a-random-secret

# Admin password for initial seed (optional — random generated if not set)
# ADMIN_PASSWORD=YourSecureP4ss!

# CORS allowed origins (comma-separated). Leave empty for "*" (dev only).
# CORS_ORIGINS=https://nexusgate.example.com
